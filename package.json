{
    "name": "@xlabs/preview-deploy",
    "version": "0.0.0",
    "scripts": {
        "build": "bash scripts/build.sh ${PWD} ${PKG_NAME:-@xlabs/portal-bridge-ui} preview",
        "unpublish": "bash scripts/unpublish.sh ${PWD} ${PKG_NAME:-@xlabs/portal-bridge-ui}"
    }
}
